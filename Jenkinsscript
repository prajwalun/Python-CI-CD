pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {

                    // Run pytest
                    sh 'pytest test_math_utils.py'
                }
            }
        }
    }

    // Additional stages or post-build actions can be defined here
}
